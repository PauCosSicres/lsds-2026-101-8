services:
  datanode1:
    build: ./datanode
    ports:
      - "8001:80"
    volumes:
      - ./datanode:/code

  datanode2:
    build: ./datanode
    ports:
      - "8002:80"
    volumes:
      - ./datanode:/code

  datanode3:
    build: ./datanode
    ports:
      - "8003:80"
    volumes:
      - ./datanode:/code
